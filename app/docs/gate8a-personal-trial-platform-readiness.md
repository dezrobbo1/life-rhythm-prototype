# Gate 8A — Personal-Trial Platform Readiness

Reviewed `main`: `5f0b7027dc60819fa9ea47cca1685c0f012cd64b` (PR #159 merged). Review date: 2026-09-26. This is current programme guidance; the PR #104-era trial guides are historical. Gates 7, 8A1 and 8A2 are complete; Gate 8A3 is merged and under bounded post-merge correction. Gate 8A4 follows only after that correction is reviewed and merged. The longitudinal Gate 8B trial has **not** begun.

## Verified baseline, delivery status and slice ownership

| Slice | Verified status or remaining product blocker | Direct evidence |
| --- | --- | --- |
| 8A1 — complete | PR #157 replaced fabricated task durations with exact authored minutes and exact preceding-variant fallback, made Capture's quiet-planning authority explicit, prevented unauthored Task-type targeting, added bounded Held/Today correction, and made definition writes repair-safe. Historic values remain preserved with visible authorship uncertainty. | `app/src/features/taskPool/taskPoolCapture.ts`, `app/src/features/taskPool/TaskDefinitionFields.tsx`, `app/src/data/taskDefinitionRepository.ts`, `app/src/domain/currentStateProjection.ts` |
| 8A2 — complete, merged PR #158 | Built-in catalogue rows are suggestions rather than personal intent. A confirmed built-in or custom template now stores exact variants and a separate durable plan and recurrence revision. Enabled plans generate deterministic, idempotent flexible-quota instances; each accepted scheduler placement retains template/plan/revision/instance identity; the instance is atomically projected into Today and owns its lifecycle and factual history. Pause, disable and re-enable preserve history. Session flags and old schedule hints are not promoted. | `app/src/data/rhythmAuthoritySchemas.ts`, `app/src/data/rhythmAuthorityRepository.ts`, `app/src/domain/rhythmRecurrence.ts`, `app/src/data/rhythmTodayRepository.ts`, `app/src/screens/LibraryScreen.tsx` |
| 8A3 | Merged PR #159 added explicit reviewed activation in Setup, durable weekday assignments and work-bound travel, and a static recurring `.ics` source. A post-merge correctness PR addresses duration-based exception zones, cross-date work boundaries and recurrence horizon edges; initial-plan settings concurrency is protected by the canonical snapshot regression. Unreviewed profiles still fall back to explicit `openCapacity` blocks. Live providers remain deferred. | `app/src/domain/calendarAvailability.ts`, `app/src/data/schedulerPlanCoordinator.ts`, `app/src/screens/SetupScreen.tsx`, `app/src/features/plan/CalendarSourceControl.tsx`, `app/src/data/calendarSourceRepository.ts` |
| 8A4 | Clerk provides sign-in/identity; canonical product rows still live in a user-namespaced Dexie IndexedDB on one browser/device. Backup checking is not restore or cross-device canonical persistence. Gate 7E duration controls need a deliberate portability contract. | `app/src/auth/AuthShell.tsx`, `app/src/data/localDataNamespace.ts`, `app/src/data/db.ts`, `app/docs/gate7-exit-report.md` |
| 8A5 | Calendar, explicit-preference, duration-control and Gate 8A1 task-definition changes have dedicated repair attention. Gate 8A2 makes every new rhythm-authority and occurrence-lifecycle write repair-safe. General status transitions, Setup/Life Shape settings now mark scheduler repair attention on scheduling changes; other older mutations still do not converge on one canonical repair coordinator. Minimum Done, Not today/Park and one-step scheduler Undo exist; individual Move/Protect and per-placement Why this time? remain incomplete. | `app/src/data/calendarSourceMutationCoordinator.ts`, `app/src/data/explicitPreferenceMutationCoordinator.ts`, `app/src/data/durationLearningControlMutationCoordinator.ts`, `app/src/data/taskDefinitionPlanReconciliation.ts`, `app/src/data/rhythmAuthorityRepository.ts`, `app/src/screens/PersonalPlanScreen.tsx` |
| 8A6 | Normal Done, Full Done and Stop reach the same persisted `done` lifecycle transition and Gate 7A `taskCompleted` event without the completed variant; an earlier Minimum Done can exist separately. Do not interpret those completions as distinct Normal/Full outcomes yet. | `app/src/screens/TodayScreen.tsx`, `app/src/data/taskLifecycleRepository.ts`, `app/src/data/behaviourEventRepository.ts` |
| 8A7 | Other product copy still needs Gate 8A7 convergence despite Setup showing a static recurring `.ics` source and automatic private scheduling. Capture/Library preview wording also overstates or obscures current scheduling authority. Full navigation and daily copy convergence remain. | `app/src/screens/SetupScreen.tsx`, `app/src/features/plan/CalendarSourceControl.tsx`, `app/src/screens/LibraryScreen.tsx` |
| 8A8 | Current Gate 8 evidence protocol, measurement, portability and actual-device end-to-end acceptance have not passed. | `MVP_PLAN.md`, `app/docs/gate7-exit-report.md` |

## 8A1 contract and legacy records

Capture means remember the task **and permit quiet private planning**, without placing it directly in Today or demanding immediate action. `captured` remains scheduler eligible. A newly authored task supplies positive whole minutes for Minimum; omitted Normal inherits the exact Minimum action and minutes, and omitted Full inherits the exact Normal action and minutes. The persisted version objects are the scheduler truth. Ad-hoc tasks have no authored Task type classification; area and per-task preferences remain available, while Task type rules apply only where a real classification exists.

Historic 5/10/20 durations are preserved as saved values, with unknown authorship. No automatic backfill or inferred provenance is justified. The correction UI warns that older values may have been filled automatically, displays the stored minutes and requires an explicit saved correction. There is no schema-wide destructive migration. Newly changed definition writes mark an accepted plan for repair within the same storage transaction; while repair is pending, automatic placements are hidden and the user can retry. The original schedule cannot safely be offered as Undo for a definition edit, since its time assumptions are obsolete; existing Undo for other valid scheduler repairs remains.

Visible defaults on the two creation paths: `area: other` (shown in the form and changeable), `kind: adhoc`, `priority: normal`, `completionStyle: flexible`, no prep/travel/cleanup/transition time, `bestTime: anytime`, `frequency: 0`, `movable/droppable: true`. `normal` priority is the neutral ranking baseline; `medium` energy is projected but currently does not rank placements, and `unclear` start barrier is not projected. `simple` is a historical classification placeholder and must not drive ad-hoc Task type preferences. Time edges are authored only when entered. No user-authored purpose is fabricated for new one-offs. Any future scheduler use of these defaults needs its own authority review.

## 8A2 implemented boundary

The coherent transition now works as: catalogue suggestion → user-confirmed template → durable plan and prospective recurrence revision → deterministic concrete instance → private scheduler placement → atomic Today projection → Start/Pause/Resume/Minimum Done/completion/skip → factual history linked to the exact instance. The first recurrence subset is flexible quota only: positive frequency per local day, Monday-start local week or local month; optional preferred weekdays; broad preferred time; positive max per day; IANA timezone; and explicit effective local date. The private-plan horizon opens intersecting recurrence periods for seven local dates. It never backfills before the effective date or the first observed horizon, and unmet quota never carries forward.

Pause and disable both stop new generation without deleting the stable plan, generated occurrences or history. Re-enable reuses the same plan. Prospective recurrence edits create monotonic revisions; already generated instance snapshots and identities remain unchanged. Template-only content edits do not create recurrence revisions. Generated occurrences are never Pool items. A Today projection explicitly references its instance, and lifecycle mutation updates both atomically. Generated-instance completions remain factual but are excluded from Gate 7E duration adaptation until Gate 8A6 gives completion variants stronger meaning.

`Add to Today once` requires a user-confirmed saved template. It reuses the earliest live generated instance for that template whose eligibility includes Today; otherwise it creates one deterministic, template-linked manual Today task for the template/date. Repetition reuses that same object. The manual path does not create or change a plan and does not consume quota.

Deferred recurrence extensions include every-N-day/week/month, anchored fortnightly or weekday cadence, hard weekday rules, anchored day of month, travel/timezone policy, and full pack activation ownership. Quick Packs are preview-only. External calendar writes remain prohibited. The schema migration to database v6 adds separate plan, recurrence-revision and instance stores without promoting fixture/session enablement, old hints, or existing Library-created Today tasks. A separate v1 rhythm-authority export/check format includes configured templates, plans, revisions, instances, linked Today projections and instance-linked factual events; restore remains unavailable.

## Trial start gate and classification

**Product blockers:** the Gate 8A3 post-merge correctness correction, remaining 8A4–8A7 gaps above, and 8A8's unpassed acceptance. Gate 8B Day 1 requires a documented 8A8 pass, current backup/recovery and evidence protocol, verified device/browser daily loop, and trustworthy scheduling and lifecycle facts. Trial outcomes must be measured without treating historic placeholders or ambiguous completions as user-authored evidence.

**Owner's Perth-only early testing:** single stable URL, one browser/device, reviewed usable-day capacity or explicit Life Shape capacity, supported recurring `.ics` files, local backups and manual feedback can support bounded prototype exploration before 8A8. Label that use as early testing and retain known limitations; it is not the longitudinal evidence trial.

**Future enhancements:** AI interpretation, notifications, inferred time preferences, richer feedback, anchored recurrence forms and a final learnt-about-me surface can wait for evidence of value. **Broader-beta requirements:** supported cross-device recovery/sync, provider coverage, operational security/privacy review, support and release processes need a separate decision beyond the owner's single-device early testing. A live provider connection or broad recurrence grammar is not automatically required for Perth-only early testing.
